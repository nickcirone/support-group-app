<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/head %> 
</head>
<body>
	<% include partials/adminNav %> 
    <!-- Header -->
    <header class="masthead bg-primary text-white text-center">
    	<div class="tab-content py-3 px-3 px-sm-0" id="nav-tabContent">
					<div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
						<div class="container">
	        				<h1 class="text-uppercase mb-0">Welcome!</h1>
	       	 				<hr class="star-light">
	        				<h2 class="font-weight-light mb-0">to Pediatric Peer to Peer Support</h2>
                    </br>
                  		<h3>
                    	<% if (user.role != 'admin') { %>
							You do not have admin permission! Please <a href="/login">log in as an administrator.</a>
				    	<% } else { %>
							<p>Hello, <%= user.username %>. This is the administrator dashboard, here you can create users and upload images for use in patient profiles.</p>
						<% } %>
                  		</h3>
      				</div>
					</div>
					
				</div>
      
    </header>
<% include partials/footer %>
</body>
<% include partials/script %>
</html>